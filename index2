<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tilt — Focus Timer</title>
  <meta name="description" content="Three gestures. Zero taps. Total focus.">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=Outfit:wght@300;400;500;600;700&display=swap" rel="stylesheet">

  <style>
    :root {
      --black:     #FAFAF8;
      --white:     #111111;
      --lime:      #111111;
      --lime-dark: #333333;
      --card:      #F1F0EC;
      --card-2:    #E8E7E2;
      --muted:     rgba(0,0,0,0.45);
      --border:    rgba(0,0,0,0.08);
    }

    *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }

    html { scroll-behavior: smooth; }

    body {
      font-family: 'Outfit', sans-serif;
      background: var(--black);
      color: var(--white);
      overflow-x: hidden;
    }

    /* ── NAV ─────────────────────────────────── */
    nav {
      position: fixed;
      top: 0; left: 0; right: 0;
      z-index: 100;
      padding: 20px 48px;
      display: flex;
      align-items: center;
      justify-content: space-between;
      transition: background 0.3s, backdrop-filter 0.3s, border-color 0.3s;
      border-bottom: 1px solid transparent;
    }

    nav.scrolled {
      backdrop-filter: blur(16px);
      -webkit-backdrop-filter: blur(16px);
    }

    nav.scrolled-hero {
      background: rgba(8,8,8,0.88);
      border-color: rgba(255,255,255,0.08);
    }

    nav.scrolled-hero .nav-logo { color: #FFFFFF; }
    nav.scrolled-hero .nav-cta  { background: #FFFFFF; color: #080808; }

    nav.scrolled-page {
      background: rgba(250,250,248,0.92);
      border-color: rgba(0,0,0,0.08);
    }

    nav.scrolled-page .nav-logo { color: #111111; }
    nav.scrolled-page .nav-cta  { background: #111111; color: #FAFAF8; }

    .nav-logo {
      font-family: 'Bebas Neue', cursive;
      font-size: 1.9rem;
      letter-spacing: 0.06em;
      color: var(--white);
      text-decoration: none;
    }

    .nav-cta {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      background: var(--lime);
      color: #FAFAF8;
      padding: 10px 22px;
      border-radius: 8px;
      text-decoration: none;
      font-size: 0.88rem;
      font-weight: 700;
      letter-spacing: 0.01em;
      transition: transform 0.2s, background 0.2s;
    }

    .nav-cta:hover { background: var(--lime-dark); transform: translateY(-1px); }

    /* ── HERO ─────────────────────────────────── */
    .hero {
      position: relative;
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: flex-end;
      padding: 80px 80px 88px;
      overflow: hidden;
    }

    .hero-bg {
      position: absolute;
      inset: 0;
      background-image: url('WebHero.png');
      background-size: cover;
      background-position: center right;
      background-repeat: no-repeat;
    }

    .hero-overlay {
      position: absolute;
      inset: 0;
      background:
        linear-gradient(to right, rgba(8,8,8,0.93) 0%, rgba(8,8,8,0.65) 45%, rgba(8,8,8,0.1) 100%),
        linear-gradient(to top, rgba(8,8,8,0.75) 0%, transparent 50%);
    }

    .hero-content {
      position: relative;
      z-index: 2;
      max-width: 720px;
    }

    .hero-label {
      font-size: 0.75rem;
      font-weight: 700;
      letter-spacing: 0.28em;
      text-transform: uppercase;
      color: rgba(255,255,255,0.5);
      margin-bottom: 16px;
      opacity: 0;
      animation: fadeUp 0.6s ease 0.2s forwards;
    }

    .hero-title {
      font-family: 'Bebas Neue', cursive;
      font-size: clamp(7rem, 20vw, 16rem);
      line-height: 0.85;
      letter-spacing: -0.01em;
      color: #FFFFFF;
      margin-bottom: 36px;
      opacity: 0;
      transform: rotate(-6deg) translateY(40px);
      animation: tiltReveal 0.9s cubic-bezier(0.16, 1, 0.3, 1) 0.4s forwards;
    }

    .hero-tagline {
      font-size: clamp(1.1rem, 2.2vw, 1.45rem);
      font-weight: 300;
      color: rgba(255,255,255,0.75);
      max-width: 460px;
      line-height: 1.55;
      margin-bottom: 52px;
      opacity: 0;
      animation: fadeUp 0.6s ease 0.9s forwards;
    }

    .hero-cta-wrap {
      display: flex;
      align-items: center;
      gap: 24px;
      opacity: 0;
      animation: fadeUp 0.6s ease 1.1s forwards;
    }

    .hero-cta {
      display: inline-flex;
      align-items: center;
      gap: 10px;
      background: #FFFFFF;
      color: #000000;
      padding: 10px 18px 10px 14px;
      border-radius: 14px;
      border: 1.5px solid #000000;
      text-decoration: none;
      transition: transform 0.25s, box-shadow 0.25s;
    }

    .hero-cta:hover {
      transform: translateY(-3px);
      box-shadow: 0 16px 48px rgba(255,255,255,0.12);
    }

    .hero-cta svg { width: 32px; height: 32px; flex-shrink: 0; }

    .hero-cta-text {
      display: flex;
      flex-direction: column;
      gap: 1px;
    }

    .hero-cta-small {
      font-size: 0.7rem;
      font-weight: 400;
      letter-spacing: 0.01em;
      line-height: 1;
      color: rgba(0,0,0,0.7);
      white-space: nowrap;
    }

    .hero-cta-large {
      font-size: 1.2rem;
      font-weight: 600;
      letter-spacing: -0.01em;
      line-height: 1;
      color: #000000;
      white-space: nowrap;
    }

    .hero-sub {
      font-size: 0.85rem;
      color: rgba(255,255,255,0.38);
    }

    .hero-email-form {
      display: flex;
      gap: 10px;
      margin-top: 28px;
      max-width: 420px;
      opacity: 0;
      animation: fadeUp 0.6s ease 1.3s forwards;
    }

    .hero-email-input {
      flex: 1;
      padding: 13px 18px;
      border: 1.5px solid rgba(255,255,255,0.25);
      border-radius: 10px;
      font-family: 'Outfit', sans-serif;
      font-size: 0.9rem;
      background: rgba(255,255,255,0.08);
      color: #fff;
      outline: none;
      transition: border-color 0.2s, background 0.2s;
    }

    .hero-email-input:focus {
      border-color: rgba(255,255,255,0.6);
      background: rgba(255,255,255,0.12);
    }

    .hero-email-input::placeholder { color: rgba(255,255,255,0.35); }

    .hero-email-btn {
      padding: 13px 22px;
      background: #fff;
      color: #111;
      border: none;
      border-radius: 10px;
      font-family: 'Outfit', sans-serif;
      font-size: 0.9rem;
      font-weight: 600;
      cursor: pointer;
      white-space: nowrap;
      transition: opacity 0.2s, transform 0.2s;
    }

    .hero-email-btn:hover { opacity: 0.88; transform: translateY(-1px); }
    .hero-email-btn:disabled { opacity: 0.5; cursor: not-allowed; transform: none; }

    .hero-email-success {
      display: none;
      margin-top: 16px;
      font-size: 0.88rem;
      color: rgba(255,255,255,0.55);
      animation: fadeUp 0.4s ease forwards;
    }

    .hero-email-success.visible { display: block; }

    @media (max-width: 768px) {
      .hero-email-form { flex-direction: column; max-width: 100%; }
      .hero-email-btn { width: 100%; }
    }

    /* ── GESTURES ─────────────────────────────── */
    .gestures {
      padding: 120px 80px;
      background: var(--card);
    }

    .section-eyebrow {
      font-size: 0.72rem;
      font-weight: 700;
      letter-spacing: 0.28em;
      text-transform: uppercase;
      color: rgba(0,0,0,0.4);
      margin-bottom: 16px;
    }

    .section-title {
      font-family: 'Bebas Neue', cursive;
      font-size: clamp(3rem, 7vw, 5.5rem);
      line-height: 0.88;
      letter-spacing: 0.01em;
    }

    .section-header { margin-bottom: 80px; }

    .gestures-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 2px;
    }

    .gesture-card {
      background: var(--card-2);
      padding: 60px 48px 56px;
      position: relative;
      overflow: hidden;
    }

    .gesture-card:first-child { border-radius: 16px 0 0 16px; }
    .gesture-card:last-child  { border-radius: 0 16px 16px 0; }

    .gesture-number {
      font-family: 'Bebas Neue', cursive;
      font-size: 6rem;
      color: rgba(0,0,0,0.05);
      position: absolute;
      top: 12px; right: 20px;
      line-height: 1;
      pointer-events: none;
      user-select: none;
    }

    .phone-wrapper {
      width: 60px;
      height: 96px;
      margin-bottom: 52px;
    }

    .phone-svg { width: 100%; height: 100%; }

    .tilt-anim   { animation: doTilt   3.2s ease-in-out infinite; }
    .rotate-anim { animation: doRotate 3.6s ease-in-out infinite 0.8s; }
    .shake-anim  { animation: doShake  2.8s ease-in-out infinite 1.6s; }

    .gesture-name {
      font-family: 'Bebas Neue', cursive;
      font-size: 3rem;
      letter-spacing: 0.03em;
      margin-bottom: 16px;
    }

    .gesture-desc {
      font-size: 0.95rem;
      font-weight: 300;
      color: var(--muted);
      line-height: 1.75;
      max-width: 250px;
    }

    /* ── FEATURES ─────────────────────────────── */
    .features {
      padding: 2px 0;
      background: var(--black);
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 2px;
    }

    .feature-block {
      background: var(--card);
      padding: 88px 72px;
      position: relative;
      overflow: hidden;
    }

    .feature-block:first-child { border-radius: 0; }
    .feature-block:last-child  { border-radius: 0; }

    .feature-icon {
      font-size: 2.8rem;
      margin-bottom: 36px;
      display: block;
      line-height: 1;
    }

    .feature-title {
      font-family: 'Bebas Neue', cursive;
      font-size: clamp(2.5rem, 5vw, 4.2rem);
      line-height: 0.88;
      margin-bottom: 28px;
    }

    .feature-desc {
      font-size: 1.02rem;
      font-weight: 300;
      color: var(--muted);
      line-height: 1.85;
      max-width: 380px;
    }

    .feature-highlight { font-weight: 600; text-decoration: underline; text-decoration-color: rgba(0,0,0,0.3); text-underline-offset: 3px; }

    /* ── SCREENSHOTS ──────────────────────────── */
    .screenshots {
      padding: 120px 0 120px;
      background: var(--black);
      overflow: hidden;
    }

    .screenshots-header {
      padding: 0 80px;
      margin-bottom: 64px;
    }

    .screenshots-scroll {
      display: flex;
      gap: 18px;
      overflow-x: auto;
      padding: 8px 80px 40px;
      scrollbar-width: thin;
      scrollbar-color: rgba(0,0,0,0.15) transparent;
      cursor: grab;
    }

    .screenshots-scroll:active { cursor: grabbing; }

    .screenshots-scroll::-webkit-scrollbar        { height: 3px; }
    .screenshots-scroll::-webkit-scrollbar-track  { background: transparent; }
    .screenshots-scroll::-webkit-scrollbar-thumb  { background: rgba(0,0,0,0.15); border-radius: 2px; }

    .screenshot-item { flex-shrink: 0; width: 255px; }

    .screenshot-item img {
      width: 100%;
      height: auto;
      border-radius: 22px;
      display: block;
      border: 1px solid rgba(0,0,0,0.08);
      transition: transform 0.35s cubic-bezier(0.16,1,0.3,1), box-shadow 0.35s;
    }

    .screenshot-item img:hover {
      transform: translateY(-10px) scale(1.02);
      box-shadow: 0 32px 80px rgba(0,0,0,0.7);
    }

    /* ── FAQ ──────────────────────────────────── */
    .faq-section {
      padding: 120px 80px;
      background: var(--card);
    }

    .faq-grid {
      margin-top: 80px;
      display: grid;
      grid-template-columns: 1fr 1fr;
      border: 1px solid var(--border);
      border-radius: 16px;
      overflow: hidden;
    }

    .faq-item {
      padding: 40px 48px;
      border-bottom: 1px solid var(--border);
      border-right: 1px solid var(--border);
    }

    .faq-item:nth-child(even)     { border-right: none; }
    .faq-item:nth-last-child(-n+2){ border-bottom: none; }

    .faq-q {
      font-size: 1rem;
      font-weight: 600;
      margin-bottom: 12px;
      line-height: 1.45;
    }

    .faq-a {
      font-size: 0.92rem;
      font-weight: 300;
      color: var(--muted);
      line-height: 1.8;
    }

    .faq-a a { color: var(--lime); text-decoration: none; }
    .faq-a a:hover { text-decoration: underline; }

    /* ── SOCIAL ───────────────────────────────── */
    .social-bar {
      padding: 40px 80px;
      background: var(--black);
      border-top: 1px solid var(--border);
      display: flex;
      align-items: center;
      justify-content: space-between;
    }

    .social-bar-label {
      font-size: 0.7rem;
      font-weight: 600;
      letter-spacing: 0.22em;
      text-transform: uppercase;
      color: rgba(0,0,0,0.3);
    }

    .social-icons {
      display: flex;
      gap: 10px;
      align-items: center;
    }

    .social-icon-btn {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 36px;
      height: 36px;
      border: 1px solid rgba(0,0,0,0.15);
      border-radius: 8px;
      text-decoration: none;
      transition: background 0.2s, border-color 0.2s;
    }

    .social-icon-btn:hover {
      background: rgba(0,0,0,0.05);
      border-color: rgba(0,0,0,0.3);
    }

    .social-icon-btn svg {
      width: 14px;
      height: 14px;
      fill: rgba(0,0,0,0.5);
      display: block;
    }

    /* ── FOOTER ───────────────────────────────── */
    footer {
      padding: 56px 80px;
      background: var(--black);
      border-top: 1px solid var(--border);
      display: flex;
      align-items: center;
      justify-content: space-between;
    }

    .footer-logo {
      font-family: 'Bebas Neue', cursive;
      font-size: 1.6rem;
      letter-spacing: 0.06em;
      color: rgba(0,0,0,0.22);
    }

    .footer-links { display: flex; gap: 32px; align-items: center; }

    .footer-links a {
      font-size: 0.85rem;
      color: rgba(0,0,0,0.4);
      text-decoration: none;
      transition: color 0.2s;
    }

    .footer-links a:hover { color: var(--white); }

    .footer-copy { font-size: 0.82rem; color: rgba(0,0,0,0.22); }

    /* ── EMAIL CAPTURE ────────────────────────── */
    .email-section {
      padding: 100px 80px;
      background: var(--card);
      text-align: center;
    }

    .email-section .section-eyebrow { margin-bottom: 12px; }
    .email-section .section-title   { margin-bottom: 48px; }

    .email-form {
      display: flex;
      gap: 12px;
      max-width: 480px;
      margin: 0 auto;
    }

    .email-input {
      flex: 1;
      padding: 14px 20px;
      border: 1.5px solid rgba(0,0,0,0.15);
      border-radius: 10px;
      font-family: 'Outfit', sans-serif;
      font-size: 0.95rem;
      background: #fff;
      color: #111;
      outline: none;
      transition: border-color 0.2s;
    }

    .email-input:focus { border-color: #111; }
    .email-input::placeholder { color: rgba(0,0,0,0.35); }

    .email-btn {
      padding: 14px 28px;
      background: #111;
      color: #FAFAF8;
      border: none;
      border-radius: 10px;
      font-family: 'Outfit', sans-serif;
      font-size: 0.95rem;
      font-weight: 600;
      cursor: pointer;
      white-space: nowrap;
      transition: background 0.2s, transform 0.2s;
    }

    .email-btn:hover { background: #333; transform: translateY(-1px); }
    .email-btn:disabled { opacity: 0.6; cursor: not-allowed; transform: none; }

    .email-success {
      display: none;
      margin-top: 16px;
      font-size: 0.9rem;
      color: rgba(0,0,0,0.5);
    }

    .email-success.visible { display: block; }

    @media (max-width: 768px) {
      .email-section { padding: 80px 24px; }
      .email-form { flex-direction: column; }
      .email-btn { width: 100%; }
    }

    /* ── KEYFRAMES ────────────────────────────── */
    @keyframes fadeUp {
      from { opacity: 0; transform: translateY(20px); }
      to   { opacity: 1; transform: translateY(0); }
    }

    @keyframes tiltReveal {
      from { opacity: 0; transform: rotate(-6deg) translateY(40px); }
      to   { opacity: 1; transform: rotate(0deg) translateY(0); }
    }

    @keyframes doTilt {
      0%, 12%  { transform: rotate(0deg); }
      35%, 65% { transform: rotate(-88deg); }
      88%, 100%{ transform: rotate(0deg); }
    }

    @keyframes doRotate {
      0%, 10%  { transform: rotate(-90deg); }
      38%, 48% { transform: rotate(0deg); }
      73%, 83% { transform: rotate(90deg); }
      100%     { transform: rotate(-90deg); }
    }

    @keyframes doShake {
      0%, 12%, 52%, 100% { transform: translateX(0) rotate(0deg); }
      17% { transform: translateX(-10px) rotate(-3deg); }
      22% { transform: translateX(10px) rotate(3deg); }
      27% { transform: translateX(-10px) rotate(-3deg); }
      32% { transform: translateX(10px) rotate(3deg); }
      37% { transform: translateX(-7px) rotate(-2deg); }
      42% { transform: translateX(7px) rotate(2deg); }
      47% { transform: translateX(0) rotate(0deg); }
    }

    /* ── SCROLL REVEAL ────────────────────────── */
    .reveal {
      opacity: 0;
      transform: translateY(32px);
      transition: opacity 0.72s ease, transform 0.72s ease;
    }

    .reveal.visible { opacity: 1; transform: translateY(0); }

    .reveal-delay-1 { transition-delay: 0.12s; }
    .reveal-delay-2 { transition-delay: 0.24s; }
    .reveal-delay-3 { transition-delay: 0.36s; }

    /* ── RESPONSIVE ───────────────────────────── */
    @media (max-width: 1024px) {
      .features {
        grid-template-columns: 1fr;
        gap: 2px;
      }
    }

    @media (max-width: 900px) {
      .gestures-grid {
        grid-template-columns: 1fr;
        gap: 2px;
      }

      .gesture-card:first-child { border-radius: 16px 16px 0 0; }
      .gesture-card:last-child  { border-radius: 0 0 16px 16px; }

      .faq-grid { grid-template-columns: 1fr; }
      .faq-item { border-right: none !important; }
      .faq-item:nth-last-child(-n+2) { border-bottom: 1px solid var(--border) !important; }
      .faq-item:last-child           { border-bottom: none !important; }
    }

    @media (max-width: 768px) {
      nav { padding: 16px 24px; }

      .hero {
        padding: 80px 24px 64px;
        min-height: 100svh;
        justify-content: flex-end;
      }

      .hero-bg {
        background-position: 35% center;
        background-size: 270%;
      }

      .hero-overlay {
        background:
          linear-gradient(to top,
            rgba(8,8,8,1)    0%,
            rgba(8,8,8,0.95) 45%,
            rgba(8,8,8,0.5)  68%,
            rgba(8,8,8,0.1)  100%
          );
      }

      .hero-title { font-size: clamp(5.5rem, 22vw, 9rem); }

      .gestures    { padding: 80px 24px; }
      .features    { padding: 0; }
      .feature-block { padding: 60px 32px; }

      .screenshots-header { padding: 0 24px; }
      .screenshots-scroll { padding: 8px 24px 40px; }
      .screenshots { padding: 80px 0; }

      .faq-section { padding: 80px 24px; }

      footer {
        padding: 40px 24px;
        flex-direction: column;
        gap: 24px;
        text-align: center;
      }

      .footer-links { justify-content: center; flex-wrap: wrap; gap: 20px; }

      .social-bar {
        padding: 28px 24px;
        flex-direction: column;
        gap: 16px;
        text-align: center;
      }
    }
  </style>
</head>
<body>

  <!-- ── NAV ───────────────────────────────────── -->
  <nav id="nav">
    <a href="#" class="nav-logo">TILT</a>
    <a href="#emailForm" class="nav-cta">Coming Soon</a>
  </nav>

  <!-- ── HERO ──────────────────────────────────── -->
  <section class="hero">
    <div class="hero-bg"></div>
    <div class="hero-overlay"></div>
    <div class="hero-content">
      <p class="hero-label">Focus Timer for iPhone</p>
      <h1 class="hero-title">TILT</h1>
      <p class="hero-tagline">Tilt to start. Rotate to switch.<br>Shake to reset. No buttons. Just focus.</p>
      <div class="hero-cta-wrap">
        <a href="#emailForm" class="hero-cta">
          <svg viewBox="0 0 24 24" fill="currentColor">
            <path d="M18.71 19.5c-.83 1.24-1.71 2.45-3.05 2.47-1.34.03-1.77-.79-3.29-.79-1.53 0-2 .77-3.27.82-1.31.05-2.3-1.32-3.14-2.53C4.25 17 2.94 12.45 4.7 9.39c.87-1.52 2.43-2.48 4.12-2.51 1.28-.02 2.5.87 3.29.87.78 0 2.26-1.07 3.8-.91.65.03 2.47.26 3.64 1.98-.09.06-2.17 1.28-2.15 3.81.03 3.02 2.65 4.03 2.68 4.04-.03.07-.42 1.44-1.38 2.83M13 3.5c.73-.83 1.94-1.46 2.94-1.5.13 1.17-.34 2.35-1.04 3.19-.69.85-1.83 1.51-2.95 1.42-.15-1.15.41-2.35 1.05-3.11z"/>
          </svg>
          <div class="hero-cta-text">
            <span class="hero-cta-small">Available soon on the</span>
            <span class="hero-cta-large">App Store</span>
          </div>
        </a>
        <span class="hero-sub">Get notified when it launches.</span>
      </div>
      <form class="hero-email-form" id="heroEmailForm">
        <input class="hero-email-input" type="email" id="heroEmailInput" placeholder="your@email.com" required>
        <button class="hero-email-btn" type="submit" id="heroEmailBtn">Notify me</button>
      </form>
      <p class="hero-email-success" id="heroEmailSuccess">You're on the list. We'll send you the link the moment it's live.</p>
      </div>
    </div>
  </section>

  <!-- ── GESTURES ───────────────────────────────── -->
  <section class="gestures">
    <div class="section-header reveal">
      <p class="section-eyebrow">How it works</p>
      <h2 class="section-title">Three gestures.<br>Total control.</h2>
    </div>

    <div class="gestures-grid">

      <div class="gesture-card reveal reveal-delay-1">
        <span class="gesture-number">01</span>
        <div class="phone-wrapper">
          <svg class="phone-svg tilt-anim" viewBox="0 0 60 100" fill="none" xmlns="http://www.w3.org/2000/svg">
            <!-- Body -->
            <rect x="3" y="1.5" width="54" height="97" rx="13" stroke="#111111" stroke-width="2"/>
            <!-- Screen surface -->
            <rect x="5" y="3.5" width="50" height="93" rx="11" fill="#111111" fill-opacity="0.04"/>
            <!-- Dynamic Island pill -->
            <rect x="18.5" y="9" width="23" height="7" rx="3.5" fill="#111111" fill-opacity="0.55"/>
            <!-- Home indicator bar -->
            <rect x="21" y="86" width="18" height="2.5" rx="1.25" fill="#111111" fill-opacity="0.3"/>
            <!-- Volume buttons left -->
            <rect x="1" y="25" width="2" height="9" rx="1" fill="#111111" fill-opacity="0.35"/>
            <rect x="1" y="38" width="2" height="9" rx="1" fill="#111111" fill-opacity="0.35"/>
            <!-- Power button right -->
            <rect x="57" y="30" width="2" height="13" rx="1" fill="#111111" fill-opacity="0.35"/>
          </svg>
        </div>
        <h3 class="gesture-name">Tilt</h3>
        <p class="gesture-desc">Tilt your phone to landscape and the timer starts. No tapping. No setup. Just tilt and go.</p>
      </div>

      <div class="gesture-card reveal reveal-delay-2">
        <span class="gesture-number">02</span>
        <div class="phone-wrapper">
          <svg class="phone-svg rotate-anim" viewBox="0 0 60 100" fill="none" xmlns="http://www.w3.org/2000/svg">
            <!-- Body -->
            <rect x="3" y="1.5" width="54" height="97" rx="13" stroke="#111111" stroke-width="2"/>
            <!-- Screen surface -->
            <rect x="5" y="3.5" width="50" height="93" rx="11" fill="#111111" fill-opacity="0.04"/>
            <!-- Dynamic Island pill -->
            <rect x="18.5" y="9" width="23" height="7" rx="3.5" fill="#111111" fill-opacity="0.55"/>
            <!-- Home indicator bar -->
            <rect x="21" y="86" width="18" height="2.5" rx="1.25" fill="#111111" fill-opacity="0.3"/>
            <!-- Volume buttons left -->
            <rect x="1" y="25" width="2" height="9" rx="1" fill="#111111" fill-opacity="0.35"/>
            <rect x="1" y="38" width="2" height="9" rx="1" fill="#111111" fill-opacity="0.35"/>
            <!-- Power button right -->
            <rect x="57" y="30" width="2" height="13" rx="1" fill="#111111" fill-opacity="0.35"/>
          </svg>
        </div>
        <h3 class="gesture-name">Rotate</h3>
        <p class="gesture-desc">Rotate 180° to switch between focus and break modes. Flow continues without interruption.</p>
      </div>

      <div class="gesture-card reveal reveal-delay-3">
        <span class="gesture-number">03</span>
        <div class="phone-wrapper">
          <svg class="phone-svg shake-anim" viewBox="0 0 60 100" fill="none" xmlns="http://www.w3.org/2000/svg">
            <!-- Body -->
            <rect x="3" y="1.5" width="54" height="97" rx="13" stroke="#111111" stroke-width="2"/>
            <!-- Screen surface -->
            <rect x="5" y="3.5" width="50" height="93" rx="11" fill="#111111" fill-opacity="0.04"/>
            <!-- Dynamic Island pill -->
            <rect x="18.5" y="9" width="23" height="7" rx="3.5" fill="#111111" fill-opacity="0.55"/>
            <!-- Home indicator bar -->
            <rect x="21" y="86" width="18" height="2.5" rx="1.25" fill="#111111" fill-opacity="0.3"/>
            <!-- Volume buttons left -->
            <rect x="1" y="25" width="2" height="9" rx="1" fill="#111111" fill-opacity="0.35"/>
            <rect x="1" y="38" width="2" height="9" rx="1" fill="#111111" fill-opacity="0.35"/>
            <!-- Power button right -->
            <rect x="57" y="30" width="2" height="13" rx="1" fill="#111111" fill-opacity="0.35"/>
          </svg>
        </div>
        <h3 class="gesture-name">Shake</h3>
        <p class="gesture-desc">Shake to reset the timer. Quick, decisive, physical. Exactly how resetting should feel.</p>
      </div>

    </div>
  </section>

  <!-- ── FEATURES ───────────────────────────────── -->
  <section class="features">
    <div class="feature-block reveal">
      <span class="feature-icon">🚫</span>
      <h2 class="feature-title">Block<br>distractions.<br>Automatically.</h2>
      <p class="feature-desc">TILT's intelligent app blocker uses iOS Screen Time to lock distracting apps the moment you start a session. <span class="feature-highlight">They unblock automatically when you take a break.</span> No willpower required.</p>
    </div>
    <div class="feature-block reveal reveal-delay-1">
      <span class="feature-icon">📊</span>
      <h2 class="feature-title">See when<br>you focus<br>best.</h2>
      <p class="feature-desc">Track completion rates, session length, and patterns over time. <span class="feature-highlight">Your productivity data, visualized clearly</span> — so you can double down on what actually works.</p>
    </div>
  </section>

  <!-- ── SCREENSHOTS ─────────────────────────────── -->
  <section class="screenshots">
    <div class="screenshots-header reveal">
      <p class="section-eyebrow">The app</p>
      <h2 class="section-title">See it in action</h2>
    </div>
    <div class="screenshots-scroll" id="screenshotsScroll">
      <div class="screenshot-item"><img src="screenshot-3.png" alt="Tilt app idle screen" loading="lazy"></div>
      <div class="screenshot-item"><img src="screenshot-4.png" alt="Tilt rotation gesture" loading="lazy"></div>
      <div class="screenshot-item"><img src="screenshot-5.png" alt="Calendar tracking" loading="lazy"></div>
      <div class="screenshot-item"><img src="screenshot-6.png" alt="Insights overview" loading="lazy"></div>
      <div class="screenshot-item"><img src="screenshot-7.png" alt="Insights patterns" loading="lazy"></div>
      <div class="screenshot-item"><img src="screenshot-8.png" alt="App blocker" loading="lazy"></div>
      <div class="screenshot-item"><img src="screenshot-9.png" alt="Color palettes" loading="lazy"></div>
    </div>
  </section>

  <!-- ── EMAIL CAPTURE ─────────────────────────── -->
  <section class="email-section reveal">
    <p class="section-eyebrow">Stay in the loop</p>
    <h2 class="section-title">Get notified when<br>it's available.</h2>
    <form class="email-form" id="emailForm">
      <input class="email-input" type="email" id="emailInput" placeholder="your@email.com" required>
      <button class="email-btn" type="submit" id="emailBtn">Notify me</button>
    </form>
    <p class="email-success" id="emailSuccess">You're on the list. We'll let you know the moment it's live.</p>
  </section>

  <!-- ── FAQ ────────────────────────────────────── -->
  <section class="faq-section">
    <div class="reveal">
      <p class="section-eyebrow">Support</p>
      <h2 class="section-title">Got questions?</h2>
    </div>
    <div class="faq-grid reveal">
      <div class="faq-item">
        <p class="faq-q">How do I start a focus timer?</p>
        <p class="faq-a">Tilt your phone to landscape. The timer starts immediately. Make sure rotation lock is off in Control Center first.</p>
      </div>
      <div class="faq-item">
        <p class="faq-q">The gestures aren't working.</p>
        <p class="faq-a">Check that rotation lock is turned off. Swipe down from the top-right corner, open Control Center, and tap the lock icon to disable it.</p>
      </div>
      <div class="faq-item">
        <p class="faq-q">How does the app blocker work?</p>
        <p class="faq-a">It uses iOS Screen Time to block selected apps during focus sessions. Apps automatically unblock on breaks. Screen Time needs to be enabled in Settings.</p>
      </div>
      <div class="faq-item">
        <p class="faq-q">How do I restore my purchase?</p>
        <p class="faq-a">Go to Preferences in Tilt, tap "Unlock Pro," then tap "Restore Purchases." Your subscription is tied to your Apple ID.</p>
      </div>
      <div class="faq-item">
        <p class="faq-q">How do I cancel my subscription?</p>
        <p class="faq-a">iPhone Settings → tap your name → Subscriptions → Tilt → Cancel Subscription. You'll keep Pro access until the billing period ends.</p>
      </div>
      <div class="faq-item">
        <p class="faq-q">Does Tilt collect my data?</p>
        <p class="faq-a">Nope. No accounts, no cloud sync, no data collection. Everything stays on your device. More help: <a href="/cdn-cgi/l/email-protection#f5969a9190b5879a8c9186949b819c94929adb969a98"><span class="__cf_email__" data-cfemail="30535f545570425f495443515e445951575f1e535f5d">[email&#160;protected]</span></a></p>
      </div>
    </div>
  </section>

  <!-- ── SOCIAL BAR ─────────────────────────────── -->
  <div class="social-bar">
    <span class="social-bar-label">Follow along</span>
    <div class="social-icons">

      <a href="https://www.tiktok.com/@coderoyd" class="social-icon-btn" title="TikTok" target="_blank">
        <svg viewBox="0 0 24 24"><path d="M19.59 6.69a4.83 4.83 0 0 1-3.77-4.25V2h-3.45v13.67a2.89 2.89 0 0 1-2.88 2.5 2.89 2.89 0 0 1-2.89-2.89 2.89 2.89 0 0 1 2.89-2.89c.28 0 .54.04.79.1V9.01a6.33 6.33 0 0 0-.79-.05 6.34 6.34 0 0 0-6.34 6.34 6.34 6.34 0 0 0 6.34 6.34 6.34 6.34 0 0 0 6.33-6.34V8.69a8.15 8.15 0 0 0 4.77 1.52V6.75a4.85 4.85 0 0 1-1-.06z"/></svg>
      </a>

      <a href="https://www.instagram.com/coderoyd_" class="social-icon-btn" title="Instagram" target="_blank">
        <svg viewBox="0 0 24 24"><path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zm0 5.838c-3.403 0-6.162 2.759-6.162 6.162s2.759 6.163 6.162 6.163 6.162-2.759 6.162-6.163c0-3.403-2.759-6.162-6.162-6.162zm0 10.162c-2.209 0-4-1.79-4-4 0-2.209 1.791-4 4-4s4 1.791 4 4c0 2.21-1.791 4-4 4zm6.406-11.845c-.796 0-1.441.645-1.441 1.44s.645 1.44 1.441 1.44c.795 0 1.439-.645 1.439-1.44s-.644-1.44-1.439-1.44z"/></svg>
      </a>

      <a href="https://www.threads.net/@coderoyd_" class="social-icon-btn" title="Threads" target="_blank">
        <svg viewBox="0 0 24 24"><path d="M12.186 24h-.007c-3.581-.024-6.334-1.205-8.184-3.509C2.35 18.44 1.5 15.586 1.472 12.01v-.017c.03-3.579.879-6.43 2.525-8.482C5.845 1.205 8.6.024 12.18 0h.014c2.746.02 5.043.725 6.826 2.098 1.677 1.29 2.858 3.13 3.509 5.467l-2.04.569c-1.104-3.96-3.898-5.984-8.304-6.015-2.91.022-5.11.936-6.54 2.717C4.307 6.504 3.616 8.914 3.589 12c.027 3.086.718 5.496 2.057 7.164 1.43 1.783 3.631 2.698 6.54 2.717 2.623-.02 4.358-.631 5.8-2.045 1.647-1.613 1.618-3.593 1.09-4.798-.31-.71-.873-1.3-1.634-1.75-.192 1.352-.622 2.446-1.284 3.272-.886 1.102-2.14 1.704-3.73 1.79-1.202.065-2.361-.218-3.259-.801-1.063-.689-1.685-1.74-1.752-2.964-.065-1.19.408-2.285 1.33-3.082.88-.76 2.119-1.207 3.583-1.291a13.853 13.853 0 0 1 3.02.142c-.126-.742-.375-1.332-.75-1.757-.513-.586-1.308-.883-2.359-.89h-.029c-.844 0-1.992.232-2.721 1.32L7.734 9.258c.87-1.44 2.464-2.297 4.425-2.308h.037c3.029.019 5.013 1.873 5.133 4.97.017.423.01.856-.021 1.288.12.06.237.124.35.193 1.216.755 2.103 1.79 2.565 2.995.704 1.815.584 4.659-1.714 6.927-1.832 1.793-4.032 2.641-7.323 2.677zm1.647-10.997c-.05 0-.1 0-.15.002-1.225.07-2.177.437-2.734.998-.39.392-.573.882-.54 1.418.06 1.092 1.088 1.799 2.693 1.71 1.143-.063 1.989-.537 2.518-1.408.387-.635.592-1.467.614-2.482a11.496 11.496 0 0 0-2.401-.238z"/></svg>
      </a>

      <a href="https://x.com/coderoyd_" class="social-icon-btn" title="X / Twitter" target="_blank">
        <svg viewBox="0 0 24 24"><path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-4.714-6.231-5.401 6.231H2.744l7.737-8.835L1.254 2.25H8.08l4.259 5.63zm-1.161 17.52h1.833L7.084 4.126H5.117z"/></svg>
      </a>

      <a href="https://www.youtube.com/@coderoyd" class="social-icon-btn" title="YouTube" target="_blank">
        <svg viewBox="0 0 24 24"><path d="M23.495 6.205a3.007 3.007 0 0 0-2.088-2.088c-1.87-.501-9.396-.501-9.396-.501s-7.507-.01-9.396.501A3.007 3.007 0 0 0 .527 6.205a31.247 31.247 0 0 0-.522 5.805 31.247 31.247 0 0 0 .522 5.783 3.007 3.007 0 0 0 2.088 2.088c1.868.502 9.396.502 9.396.502s7.506 0 9.396-.502a3.007 3.007 0 0 0 2.088-2.088 31.247 31.247 0 0 0 .5-5.783 31.247 31.247 0 0 0-.5-5.805zM9.609 15.601V8.408l6.264 3.602z"/></svg>
      </a>

      <a href="https://www.producthunt.com/@coderoyd" class="social-icon-btn" title="Product Hunt" target="_blank">
        <svg viewBox="0 0 24 24"><path d="M13.604 8.4h-3.405V12h3.405a1.8 1.8 0 0 0 0-3.6zM12 0C5.373 0 0 5.372 0 12s5.373 12 12 12 12-5.372 12-12S18.627 0 12 0zm1.604 14.4H10.2v3.6H7.8V6h5.804a4.2 4.2 0 0 1 0 8.4z"/></svg>
      </a>

    </div>
  </div>

  <!-- ── FOOTER ──────────────────────────────────── -->
  <footer>
    <span class="footer-logo">TILT</span>
    <div class="footer-links">
      <a href="https://gist.github.com/coderoyd/c78fad6a09378ad0b7080ad446a0f75b/raw/6ca2228c8eb3b68ba1299eed01af04736d11a7a2/tilt-privacy-policy.md">Privacy Policy</a>
      <a href="/cdn-cgi/l/email-protection#c4a7aba0a184b6abbda0b7a5aab0ada5a3abeaa7aba9">Contact</a>
      <a href="https://apps.apple.com/app/tilt-focus-timer/id6758234044">App Store</a>
    </div>
    <span class="footer-copy">© 2026 Royd Santiago</span>
  </footer>

  <script data-cfasync="false" src="/cdn-cgi/scripts/5c5dd728/cloudflare-static/email-decode.min.js"></script><script>
    // Nav: dark over hero, light over rest of page
    const nav = document.getElementById('nav');
    const hero = document.querySelector('.hero');
    function updateNav() {
      const heroBottom = hero.offsetTop + hero.offsetHeight;
      const scrolled = window.scrollY > 60;
      nav.classList.toggle('scrolled', scrolled);
      if (!scrolled) {
        nav.classList.remove('scrolled-hero', 'scrolled-page');
      } else if (window.scrollY + nav.offsetHeight < heroBottom) {
        nav.classList.add('scrolled-hero');
        nav.classList.remove('scrolled-page');
      } else {
        nav.classList.add('scrolled-page');
        nav.classList.remove('scrolled-hero');
      }
    }
    window.addEventListener('scroll', updateNav, { passive: true });
    updateNav();

    // Scroll reveal via IntersectionObserver
    const revealEls = document.querySelectorAll('.reveal');
    const io = new IntersectionObserver((entries) => {
      entries.forEach(e => { if (e.isIntersecting) e.target.classList.add('visible'); });
    }, { threshold: 0.1 });
    revealEls.forEach(el => io.observe(el));

    // Drag-to-scroll for screenshots gallery
    const scroll = document.getElementById('screenshotsScroll');
    let isDown = false, startX, scrollLeft;

    scroll.addEventListener('mousedown', e => {
      isDown = true;
      scroll.style.userSelect = 'none';
      startX = e.pageX - scroll.offsetLeft;
      scrollLeft = scroll.scrollLeft;
    });
    document.addEventListener('mouseup', () => {
      isDown = false;
      scroll.style.userSelect = '';
    });
    scroll.addEventListener('mousemove', e => {
      if (!isDown) return;
      e.preventDefault();
      const x = e.pageX - scroll.offsetLeft;
      scroll.scrollLeft = scrollLeft - (x - startX) * 1.4;
    });

    // Loops email capture
    const emailForm = document.getElementById('emailForm');
    const emailInput = document.getElementById('emailInput');
    const emailBtn = document.getElementById('emailBtn');
    const emailSuccess = document.getElementById('emailSuccess');

    emailForm.addEventListener('submit', async (e) => {
      e.preventDefault();
      const email = emailInput.value.trim();
      if (!email) return;
      emailBtn.disabled = true;
      emailBtn.textContent = 'Sending...';
      try {
        const body = new URLSearchParams({ email });
        const res = await fetch('https://app.loops.so/api/newsletter-form/cmnuc155l1woh0izoqu71pwzm', {
          method: 'POST',
          headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
          body: body.toString()
        });
        if (res.ok) {
          emailForm.style.display = 'none';
          emailSuccess.classList.add('visible');
        } else {
          emailBtn.disabled = false;
          emailBtn.textContent = 'Notify me';
        }
      } catch {
        emailBtn.disabled = false;
        emailBtn.textContent = 'Notify me';
      }
    });

  </script>
</body>
</html>

<script>
    // Hero email capture
    const heroForm = document.getElementById('heroEmailForm');
    const heroInput = document.getElementById('heroEmailInput');
    const heroBtn = document.getElementById('heroEmailBtn');
    const heroSuccess = document.getElementById('heroEmailSuccess');

    heroForm.addEventListener('submit', async (e) => {
      e.preventDefault();
      const email = heroInput.value.trim();
      if (!email) return;
      heroBtn.disabled = true;
      heroBtn.textContent = 'Sending...';
      try {
        const body = new URLSearchParams({ email });
        const res = await fetch('https://app.loops.so/api/newsletter-form/cmnuc155l1woh0izoqu71pwzm', {
          method: 'POST',
          headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
          body: body.toString()
        });
        if (res.ok) {
          heroForm.style.display = 'none';
          heroSuccess.classList.add('visible');
        } else {
          heroBtn.disabled = false;
          heroBtn.textContent = 'Notify me';
        }
      } catch {
        heroBtn.disabled = false;
        heroBtn.textContent = 'Notify me';
      }
    });
</script>
